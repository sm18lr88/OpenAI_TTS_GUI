# MSIX Visual Assets

The local MSIX build script stages placeholder PNG assets with these manifest names:

- `Square44x44Logo.png`
- `Square150x150Logo.png`
- `Wide310x150Logo.png`
- `StoreLogo.png`

Replace the generated placeholders in the staging directory with final artwork before using an MSIX for any public distribution.
